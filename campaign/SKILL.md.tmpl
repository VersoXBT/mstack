---
name: m-campaign
preamble-tier: 3
version: 1.0.0
description: |
  Integrated campaign builder for marketing operators. Turns a goal, offer, audience,
  and channel mix into a campaign brief, messaging map, asset list, launch sequence,
  measurement plan, and risk checklist. Use when asked to plan a campaign, coordinate
  multi-channel marketing, or turn a strategy into execution.
allowed-tools:
  - Bash
  - Read
  - Write
  - AskUserQuestion
---

{{PREAMBLE}}

{{BRAND_CONTEXT}}

{{LEARNINGS_SEARCH}}

# Campaign Builder

## Inputs

Identify the campaign frame:
- Business goal: pipeline, signups, retention, reactivation, waitlist, revenue, awareness.
- Audience segment and buying stage.
- Offer, promise, proof, and objection.
- Channels: email, paid social, organic social, SEO/content, landing page, partners, community.
- Dates, budget, owner, and constraints.
- North-star metric and baseline.
- Launch window, final decision date, and rollback condition.
- Compliance category and review owner if claims are regulated.

If any input is missing, draft with explicit assumptions unless it would change the
offer, audience, compliance posture, or launch date.

Check for existing campaign inputs before drafting:

```bash
find . -name "*campaign*" -o -name "*strategy*" -o -name "*landing*" -o -name "*report*" 2>/dev/null | head -10
```

Ask one bundled question only for material gaps. Do not ask for information that
can be inferred from brand context, prior learnings, or existing campaign files.

## Workflow

1. Define the campaign bet in one sentence.
2. Build the message map:
   - Core promise.
   - Primary pain.
   - Proof points.
   - Objections and replies.
   - CTA.
3. Create channel roles:
   - Capture intent.
   - Create demand.
   - Nurture.
   - Convert.
   - Retarget or follow up.
4. Produce the asset list with owner, purpose, status, and dependency.
5. Create a launch sequence with dates or relative timing.
6. Define measurement:
   - North-star campaign metric.
   - Channel metrics.
   - Leading indicators.
   - Decision thresholds.
   - Baseline and target.
   - Source of truth.
   - Instrumentation needed.
   - Review cadence.
   - Owner.
7. Add a risk checklist:
   - Message risk.
   - Audience mismatch.
   - Tracking gap.
   - Legal/compliance risk.
   - Creative production bottleneck.
8. Save the execution plan to the default path unless the user asks otherwise:
   `campaigns/{campaign-slug}-{date}.md`.

## Asset Matrix

Use this table:

| Asset | Channel | Purpose | Message angle | Format/spec | CTA | Proof needed | Owner | Due | Status | Dependency | Acceptance criteria |
|-------|---------|---------|---------------|-------------|-----|--------------|-------|-----|--------|------------|---------------------|

Statuses: `needed`, `drafting`, `review`, `approved`, `scheduled`, `live`.

## Measurement Matrix

Use this table:

| Metric | Baseline | Target | Source | Instrumentation needed | Review cadence | Owner | Decision rule |
|--------|----------|--------|--------|------------------------|----------------|-------|---------------|

Decision rules must say what to scale, pause, revise, or kill. Avoid vague
thresholds like "good engagement" unless the user has no baseline; in that case,
mark the plan directional.

## Risk Register

Use this table:

| Risk | Trigger | Severity | Mitigation | Owner | Fallback |
|------|---------|----------|------------|-------|----------|

## Output Format

Return:
- Campaign bet.
- Audience and offer.
- Message map.
- Channel plan.
- Asset matrix.
- Launch sequence.
- Measurement plan.
- Risks and fixes.
- Next three actions.
- Save path and open assumptions.

{{PRIVACY_NOTE}}

## Completion

End with `DONE`, `DONE_WITH_CONCERNS`, `BLOCKED`, or `NEEDS_CONTEXT`.
