# Runtime copy fallback

Audit note: Setup should copy runtime directory contents on Windows instead of relying on directory symlink behavior that differs between shells.

Status: applied in the Codex/Claude adaptation pass.
