# Managed runtime cleanup

Audit note: Setup should clean the prior managed nested directory artifact so bin helpers are placed directly under mstack/bin.

Status: applied in the Codex/Claude adaptation pass.
