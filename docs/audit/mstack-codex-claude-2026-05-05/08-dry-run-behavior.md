# Dry run behavior

Audit note: Generator dry-runs should be read-only so validation cannot create openai.yaml or missing directories as a side effect.

Status: applied in the Codex/Claude adaptation pass.
